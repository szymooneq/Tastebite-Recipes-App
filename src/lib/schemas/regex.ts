// No special characters
const NO_SPECIAL_CHARACTERS = /^[A-Za-z0-9 ]+$/

// File size 1Mb
const FILE_SIZE = 1024000

// Supported formats: jpg, jpeg, gif, png
const IMAGE_FORMATS = ['image/jpg', 'image/jpeg', 'image/gif', 'image/png']

export { NO_SPECIAL_CHARACTERS, FILE_SIZE, IMAGE_FORMATS }
